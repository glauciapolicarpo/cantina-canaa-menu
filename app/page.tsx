"use client";
import { useMemo, useState } from "react";

const products = [{ id: "salgado", name: "Salgado", price: 6, emoji: "🥟" }, { id: "bolo", name: "Bolo", price: 6, emoji: "🍰" }, { id: "doce", name: "Doce", price: 6, emoji: "🍬" }, { id: "refri", name: "Refri", price: 2, emoji: "🥤" }];
const pixEmail = "criatividadeedesigner@gmail.com";
const money = (value: number) => value.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
const whatsappNumber = "5511979673253";
const pixField = (id: string, value: string) => id + String(value.length).padStart(2, "0") + value;
function crc16(value: string) { let crc = 0xffff; for (let i = 0; i < value.length; i++) { crc ^= value.charCodeAt(i) << 8; for (let j = 0; j < 8; j++) crc = (crc & 0x8000) ? (crc << 1) ^ 0x1021 : crc << 1; crc &= 0xffff; } return crc.toString(16).toUpperCase().padStart(4, "0"); }
function pixPayload(amount: number, reference: string) { const merchant = pixField("00", "br.gov.bcb.pix") + pixField("01", pixEmail); const payload = pixField("00", "01") + pixField("26", merchant) + pixField("52", "0000") + pixField("53", "986") + pixField("54", amount.toFixed(2)) + pixField("58", "BR") + pixField("59", "CANTINA CANAA") + pixField("60", "SAO PAULO") + pixField("62", pixField("05", reference.slice(0, 25))) + "6304"; return payload + crc16(payload); }

export default function Home() {
  const [quantities, setQuantities] = useState<Record<string, number>>({}); const [inventory, setInventory] = useState<Record<string, number>>({ salgado: 20, bolo: 20, doce: 20, refri: 20 }); const [adminEmail, setAdminEmail] = useState(""); const [adminPassword, setAdminPassword] = useState(""); const [adminAuthenticated, setAdminAuthenticated] = useState(false); const [adminError, setAdminError] = useState(""); const [step, setStep] = useState<"menu" | "receipt" | "payment" | "admin">("menu"); const [name, setName] = useState(""); const [changeFor, setChangeFor] = useState(""); const [paymentMethod, setPaymentMethod] = useState<"cash" | "pix">("pix"); const [receipt, setReceipt] = useState(""); const [receiptLink, setReceiptLink] = useState(""); const [reference, setReference] = useState(""); const [error, setError] = useState(""); const [isSubmitting, setIsSubmitting] = useState(false);
  const selected = useMemo(() => products.filter(p => (quantities[p.id] ?? 0) > 0).map(p => ({ ...p, quantity: quantities[p.id] })), [quantities]); const total = selected.reduce((sum, item) => sum + item.price * item.quantity, 0); const count = selected.reduce((sum, item) => sum + item.quantity, 0); const changeAmount = paymentMethod === "cash" && changeFor ? Math.max(0, Number(changeFor) - total) : 0;
  const change = (id: string, amount: number) => setQuantities(q => ({ ...q, [id]: Math.max(0, Math.min(inventory[id] ?? 0, (q[id] ?? 0) + amount)) }));
  const adminLogin = (event: React.FormEvent) => { event.preventDefault(); if (adminEmail.toLowerCase() === "criatividadeedesigner@gmail.com" && adminPassword === "love") { setAdminAuthenticated(true); setAdminError(""); } else setAdminError("E-mail ou senha incorretos."); };
  const adjustStock = (id: string, amount: number) => setInventory(stock => ({ ...stock, [id]: Math.max(0, (stock[id] ?? 0) + amount) }));
  const sendToWhatsApp = () => { const summary = selected.map(i => `${i.quantity}x ${i.name}`).join(", "); const message = `Olá! Pedido ${reference} de ${name}. Itens: ${summary}. Total: ${money(total)}. Comprovante: ${receiptLink || receipt || "vou anexar o arquivo diretamente nesta conversa"}.`; window.open(`https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`, "_blank"); };
  const qrUrl = reference ? `https://quickchart.io/qr?size=240&text=${encodeURIComponent(pixPayload(total, reference))}` : "";
  const submitText = isSubmitting ? "AGUARDE, REGISTRANDO PEDIDO..." : "REGISTRAR PEDIDO E PAGAR";
  const submitIcon = isSubmitting ? "" : "→";
  const changeMessage = changeFor && Number(changeFor) >= total ? `Você vai receber: ${money(changeAmount)} de troco` : "";
  async function createOrder() { setError(""); if (!name.trim()) return setError("Informe seu nome para identificar o pedido."); if (paymentMethod === "cash" && (!changeFor || Number(changeFor) < total)) return setError("Informe um valor de troco maior ou igual ao total do pedido."); setIsSubmitting(true); try { const response = await fetch("/api/orders", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ customerName: name, items: selected, totalCents: total * 100, paymentMethod, changeForCents: paymentMethod === "cash" ? Math.round(Number(changeFor) * 100) : null, receiptText: receipt, receiptLink }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Não foi possível registrar o pedido."); setReference(data.order.reference); setStep("payment"); } catch (e) { setIsSubmitting(false); setError(e instanceof Error ? e.message : "Não foi possível registrar o pedido."); } }
  if (step === "admin" && !adminAuthenticated) return <main className="app-shell"><header className="topbar"><div className="brand-mark">C</div><div><p className="eyebrow">Área restrita</p><h1>Cantina Canaã do Brasil</h1></div></header><section className="payment-screen"><div className="payment-card form-card"><div><p className="eyebrow accent">Acesso administrativo</p><h2>Entrar no<br/><em>painel.</em></h2><p>Use o e-mail autorizado para gerenciar o estoque.</p></div><form onSubmit={adminLogin}><label>E-mail<input type="email" value={adminEmail} onChange={e => setAdminEmail(e.target.value)} required /></label><label>Senha<input type="password" value={adminPassword} onChange={e => setAdminPassword(e.target.value)} required /></label>{adminError && <p className="form-error">{adminError}</p>}<button className="primary-button" type="submit">Acessar painel <span>→</span></button></form></div></section></main>;
  if (step === "admin" && adminAuthenticated) return <main className="app-shell"><header className="topbar"><div className="brand-mark">C</div><div><p className="eyebrow">Administração</p><h1>Cantina Canaã do Brasil</h1></div><button className="help" onClick={() => { setAdminAuthenticated(false); setStep("menu"); }}>Sair</button></header><section className="payment-screen"><div className="admin-heading"><div><p className="eyebrow accent">Painel de estoque</p><h2>Gerencie seus<br/><em>itens.</em></h2></div><button className="back" onClick={() => setStep("menu")}>← Ver pedido</button></div><div className="stock-grid">{products.map(p => <article className="stock-card" key={p.id}><div className="food-icon">{p.emoji}</div><h3>{p.name}</h3><p>Disponível agora</p><strong>{inventory[p.id] ?? 0} unidades</strong><div className="stock-actions"><button onClick={() => adjustStock(p.id, -1)}>-</button><button onClick={() => adjustStock(p.id, 1)}>+</button></div></article>)}</div><p className="form-note">Estoque salvo nesta sessão de protótipo.</p></section></main>;
  if (step === "admin") return <main className="app-shell"><header className="topbar"><div className="brand-mark">C</div><div><p className="eyebrow">Administração</p><h1>Cantina Canaã do Brasil</h1></div></header><section className="payment-screen"><button className="back" onClick={() => setStep("menu")}>← Voltar ao pedido</button><div className="payment-card admin-card"><div><p className="eyebrow accent">Agenda de pedidos</p><h2>Pedidos<br/><em>recebidos.</em></h2><p>Esta área lê os pedidos persistidos no backend/D1 quando a base estiver configurada.</p></div><div className="admin-list"><p className="empty">Nenhum pedido carregado nesta sessão.</p><button className="primary-button" onClick={() => location.reload()}>Atualizar pedidos</button></div></div></section></main>;
  return <main className="app-shell"><header className="topbar"><div className="brand-mark">C</div><div><p className="eyebrow">Faça seu pedido</p><h1>Cantina Canaã do Brasil</h1></div><button className="help" onClick={() => setStep("admin")} aria-label="Abrir administração">⌘</button></header>{step === "menu" && <><section className="hero"><div><p className="eyebrow accent">Lanches e doces · preço fácil</p><h2>FAÇA SEU<br/><em>PEDIDO.</em></h2><p className="hero-copy">Escolha a quantidade, confira o total e pague via Pix.</p></div><div className="hero-art">✦<span>feito<br/>com carinho</span></div></section><div className="content-grid"><section className="menu-area"><div className="section-heading"><div><p className="eyebrow">Opções disponíveis</p><h3>Escolha seus itens</h3></div><span className="open-tag">● Aberto agora</span></div><div className="menu-list">{products.map(p => <article className="menu-item" key={p.id}><div className="food-icon">{p.emoji}</div><div className="item-info"><span className="item-category">Por unidade</span><h4>{p.name} {p.id === "refri" && <small className="discount">preço menor</small>}</h4><p>{p.id === "refri" ? "Lata 350 ml" : "R$ 6,00 por unidade"}</p><strong>{money(p.price)}</strong></div><div className="qty product-qty"><button onClick={() => change(p.id, -1)}>-</button><b>{quantities[p.id] ?? 0}</b><button onClick={() => change(p.id, 1)}>+</button></div></article>)}</div></section><aside className="cart-card"><div className="cart-header"><div><p className="eyebrow">Sua sacola</p><h3>{count} {count === 1 ? "item" : "itens"}</h3></div><span className="cart-dot">{count}</span></div><div className="cart-items">{selected.length ? selected.map(i => <div className="cart-line" key={i.id}><div><h4>{i.quantity}x {i.name}</h4><span>{money(i.price)} cada</span></div><strong>{money(i.price * i.quantity)}</strong></div>) : <p className="empty">Escolha os itens ao lado.</p>}</div><div className="cart-total"><span>Total final</span><strong>{money(total)}</strong></div><button className="primary-button" disabled={!count} onClick={() => setStep("receipt")}>Continuar <span>→</span></button></aside></div></>}{step === "receipt" && <section className="payment-screen"><button className="back" onClick={() => setStep("menu")}>← Voltar para a sacola</button><div className="payment-card form-card"><div><p className="eyebrow accent">Antes do pagamento</p><h2>Escreva<br/><em>seu nome</em><small className="name-hint">para validar na sua comanda</small></h2><p>Se o pagamento for via Pix, você deverá anexar o comprovante diretamente no WhatsApp.</p></div><form onSubmit={e => { e.preventDefault(); createOrder(); }}><label>Escreva seu nome<input value={name} onChange={e => setName(e.target.value)} placeholder="Ex.: Maria Silva" required /></label><fieldset className="payment-options"><legend>Escolha a forma de pagamento</legend><label className="payment-option"><input type="radio" name="paymentMethod" value="cash" checked={paymentMethod === "cash"} onChange={() => setPaymentMethod("cash")} /> À vista</label><label className="payment-option"><input type="radio" name="paymentMethod" value="pix" checked={paymentMethod === "pix"} onChange={() => setPaymentMethod("pix")} /> Pix</label></fieldset>{paymentMethod === "cash" && <label>Valor pago em dinheiro<input type="number" min={total} step="0.01" value={changeFor} onChange={e => setChangeFor(e.target.value)} placeholder={`Ex.: ${money(total + 10)}`} required /><small className="change-result">{changeMessage}</small></label>}<p className="form-note"></p>{error && <p className="form-error">{error}</p>}<button className="primary-button" type="submit" disabled={isSubmitting}>{submitText} <span className={isSubmitting ? "loading-spinner" : ""} aria-hidden={isSubmitting}>{submitIcon}</span></button></form></div></section>}{step === "payment" && <section className="payment-screen"><div className="payment-card"><div><p className="eyebrow accent">Pedido {reference}</p><h2>{paymentMethod === "pix" ? <>Pix<br/><em>dinâmico.</em></> : <>Pagamento<br/><em>à vista.</em></>}</h2><p>{paymentMethod === "pix" ? "O pedido foi registrado com referência única. Este QR Code Pix contém o valor e a chave informados." : `Seu pedido foi registrado para pagamento à vista na retirada ou entrega. Troco para: ${money(Number(changeFor))}.`}</p>{paymentMethod === "cash" && <p className="cash-instruction">PAGUE E RECEBA SUA COMANDA COM OS COORDENADORES</p>}<div className="amount-label">Total do pedido</div><div className="amount">{money(total)}</div>{paymentMethod === "pix" && <div className="not-configured">Confirme seu pagamento para retirar seu pedido</div>}</div><div className="qr-side">{paymentMethod === "pix" ? <><div className="qr-callout">Pague escaneando o QR-Code</div><img className="real-qr" src={qrUrl} alt={`QR Code Pix do pedido ${reference}`} /><p>Pix no valor de <strong>{money(total)}</strong><br/>chave: {pixEmail}<br/>referência: {reference}</p><button className="whatsapp" onClick={sendToWhatsApp}>CLIQUE AQUI E ENVIE O COMPROVANTE DE PAGAMENTO</button><p className="receipt-warning">APÓS O PAGAMENTO VIA PIX, ENVIE O COMPROVANTE CLICANDO NO BOTÃO VERDE QUE TE DIRECIONA PARA NOSSO NÚMERO DE WHATSAPP</p><small className="whatsapp-hint">Ao abrir, anexe o comprovante manualmente pelo clipe.</small></> : <><p className="cash-note">Pagamento combinado à vista. Nenhum QR Code será gerado.</p><button className="whatsapp" onClick={sendToWhatsApp}>CLIQUE AQUI E INFORME SEU PEDIDO NO WHATSAPP</button></>}</div></div></section>}<footer><span>Cantina Canaã do Brasil</span><span>Pedido seguro e identificado.</span></footer></main>;
}
